export type AlertBannerTypes = {
  variant?: "error" | "info"; // Optional prop for the alert type, either "error" or "info"
  title: string; // Title of the alert banner
  description: string; // Description of the alert banner
};
