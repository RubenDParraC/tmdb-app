// types
import type { ActorInterface } from "@/app/interfaces";

// The ActorItemTypes type defines the properties received by the ActorItem component
export type ActorItemTypes = {
  // actor is an object that follows the structure defined in ActorInterface
  actor: ActorInterface;
};
